<Directory "/var/www/admincp">
        AuthType Basic
        AuthName "Forum Protected Area Admin"
        AuthUserFile /etc/apache2/secure/.htpasswd_neo
        Require valid-user
</Directory>

<Directory "/var/www/admincp">
        AuthType Basic
        AuthName "Forum Protected Area Admin"
        AuthUserFile /etc/apache2/secure/.htpasswd_neo
        Require valid-user
</Directory>
